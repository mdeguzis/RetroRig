To replace: (in progress)


sed  -i '/NOVEMBER/r file2.txt' file1.txt |sed '/NOVEMBER/d'

This will replace the temp emulator line with the contents of the enabled emulator
in whatever txt file is called when the user enables the consoles.

FOR FUTURE USE ONLY.
